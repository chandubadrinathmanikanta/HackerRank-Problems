#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void print_natural_numbers(int current, int N){
        if(current > N) return;
        printf("%d ", current);
        print_natural_numbers(current + 1, N);
    }
int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int N;
    scanf("%d", &N);
    print_natural_numbers(1, N);
    return 0;
    
}
